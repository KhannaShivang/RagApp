{
  "version": 2,
  "builds": [
    {
      "src": "api/main.py",
      "use": "@vercel/python"
    }
  ],
  "buildCommand": "pip install -r requirements.txt",
  "routes": [
    { "src": "/(.*)", "dest": "api/main.py" }
  ]
}
